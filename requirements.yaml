name: echi-listeningtest
channels:
    - conda-forge
dependencies:
    - python=3.11
    - pip
    - pip:
        - jiwer
        - matplotlib
        - soundfile
        - gdown
        - hydra-core==1.3.2
